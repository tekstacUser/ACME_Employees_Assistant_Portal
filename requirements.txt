# --- Original app dependencies ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
transformers>=4.40.0
torch>=2.2.0

# --- New: Vector store (RAG pipeline deployment & vector store operations) ---
chromadb>=0.5.0
sentence-transformers>=2.7.0

# --- New: Prompt versioning & registry management ---
# (uses Python's built-in sqlite3 - no extra package required)

# --- New: MLflow LLM tracking extension ---
mlflow>=2.13.0
